# behat.yml
default:
  autoload:
    '': '%paths.base%/tests'
  formatters:
    progress: true
    pretty: true
  suites:
    default:
      paths:
        - '%paths.base%/features/'
      contexts:
        - Features\SubscriberManagerContext